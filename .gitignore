.venv/*

*temp*
dot_gitconfig.secret
dot_gitconfig.secret-noreply
dot_zprofile.secret
dot_zshrc.secret

## claude
settings.local.json

## rulesync 配布先（ソースは .rulesync/rules/ 配下。詳細は docs/rulesync.md）
## このリポジトリ内で追跡されるのは現状 CLAUDE.md のみ。
## グローバル設定 (dot_config/rulesync/) の配布先 (~/.claude, ~/.codex, ~/.copilot 等) は
## リポジトリ外の実ホームディレクトリに書き出されるため、そもそも追跡対象外。
/CLAUDE.md

## VSCode
mcp.json
!.rulesync/mcp.json
!.vscode/mcp.json
!dot_config/rulesync/exact_dot_rulesync/mcp.json
