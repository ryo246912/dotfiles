.venv/*

## mise lockfiles（Renovate の artifact 更新では生成しない）
mise*.lock

*temp*
config/git/config.secret
config/git/config.work.secret
config/zsh/.zprofile.secret
config/zsh/.zshrc.secret

## claude
settings.local.json

## rulesync 配布先（ソースは .rulesync/rules/ 配下）
/CLAUDE.md

## VSCode
mcp.json
!.rulesync/mcp.json
!.vscode/mcp.json
# templates/rulesync/mcp.json.tera は拡張子が .tera のため上の `mcp.json` ignore に
# そもそもマッチしない（旧 dot_config/.../mcp.json.tmpl 時代の !negation は不要になった）
