# Copy this template to a repository as lefthook.yml (or point
# LEFTHOOK_CONFIG at it), then uncomment and customize repository-specific
# hooks. The shared AI checks continue to come from global.yml.
extends:
  - ~/.config/lefthook/global.yml
#
# pre-commit:
#   jobs:
#     - name: repository pre-commit
#       run: mise run lint -- {staged_files}
#
# pre-push:
#   jobs:
#     - name: repository pre-push
#       run: mise run test
