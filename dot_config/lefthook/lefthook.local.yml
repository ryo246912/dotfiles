# This template is copied to the mounted repository root as lefthook.local.yml.
# Uncomment and customize repository-specific hooks as needed. The shared AI
# checks continue to come from global.yml.
extends:
  - ~/.config/lefthook/global.yml
#
# pre-commit:
#   jobs:
#     - name: repository pre-commit
#       run: mise run lint -- {staged_files}
#
# pre-push:
#   jobs:
#     - name: repository pre-push
#       run: mise run test
