[tools]
"aqua:Songmu/laminate"   = "0.0.4"
"aqua:k1LoW/deck"        = "1.24.1"
"aqua:supabase/cli"      = "2.100.1"
"github:render-oss/cli"  = { version = "2.15.1", rename_exe = "render" }
"github:superfly/flyctl" = "0.4.29"

# keycastr/tailscale-app/termius/thunderbird は auto_updates（tailscale-app は
# pkg インストーラの sudo も）を使っており mise の brew-cask シムが未対応/hang するため、
# ここではなく dot_config/mise/tasks/bootstrap-mac.toml 側で real brew から導入する
