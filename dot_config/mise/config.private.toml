[tools]
"aqua:Songmu/laminate"   = "0.0.4"
"aqua:k1LoW/deck"        = "1.24.1"
"aqua:supabase/cli"      = "2.100.1"
"github:render-oss/cli"  = { version = "2.15.1", rename_exe = "render" }
"github:superfly/flyctl" = "0.4.29"

# private ロールは mac（ryo-mac-private）と linux（ryo-win, WSL）の両ホストで共有される
# （dot_config/zsh/host-env.map 参照）。[bootstrap.packages] に brew/brew-cask のエントリを
# 置くと WSL ホストでも読み込まれてしまうため、mac 専用パッケージは置かず
# dot_config/mise/tasks/bootstrap-mac.toml の private ホスト限定 case ブロックに寄せている
# （android-studio/insomnia/linear/omnidisksweeper/ffmpeg）
