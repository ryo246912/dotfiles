#!/bin/sh

set -eu

mise_data_dir="${MISE_DATA_DIR:-${XDG_DATA_HOME:-$HOME/.local/share}/mise}"
md_preview="$mise_data_dir/shims/md-preview"

if [ ! -x "$md_preview" ]; then
  echo "md-preview is not installed. Run 'mise install' first." >&2
  exit 127
fi

exec "$md_preview" "$@"
